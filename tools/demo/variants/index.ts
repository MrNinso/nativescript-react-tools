import { DemoSharedBase } from '../utils';
import {} from '@nativescript-react-tools/variants';

export class DemoSharedVariants extends DemoSharedBase {
  testIt() {
    console.log('test variants!');
  }
}
